#!/usr/bin/env bash
if (($(getcurrentws) == 0)); then
	if xdotool getwindowfocus getwindowpid; then
		i3-msg "workspace back_and_forth"
	else
		exec "$TERMINAL"
	fi
else
	i3-msg "workspace 0"
	xdotool getwindowfocus getwindowpid || exec "$TERMINAL"
fi
