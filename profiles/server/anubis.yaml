bots:
  - import: (data)/meta/default-config.yaml

  - name: always-challenge
    expression:
      any:
        - "true"
    action: CHALLENGE
    challenge:
      algorithm: fast
      difficulty: 5
      report_as: 5
